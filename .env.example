# Supabase — copiar do painel do projeto em supabase.com
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=      # apenas no backend (api/) — nunca expor no frontend

# IAs (fallback em cascata: Groq → Gemini → HuggingFace)
GROQ_API_KEY=
GEMINI_API_KEY=
HUGGINGFACE_API_KEY=

# OAuth Google e GitHub são configurados direto no Supabase Dashboard
# Não são necessárias variáveis extras aqui para OAuth
